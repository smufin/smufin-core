[core]
input = ./input/00-fastq
data = ../data
exec = count:run;filter:run,dump;merge:run;group:run,stats

[count]
table-size = 100000000
cache-size = 1000000000

[filter]
format = plain

# vim: ft=dosini
